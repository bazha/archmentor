# Bilingual i18n — Phase A Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make ArchMentor's UI fully bilingual (RU/EN) and convert content to a localized `{ru,en}` shape, with English content seeded as a placeholder copy of Russian (real EN translations come in Phase B).

**Architecture:** A `Lang` setting in the Zustand store (auto-detected from the browser, persisted). A dependency-free message catalog + `useT()` hook translate all UI strings. Content prose fields become `{ru,en}` (and `{ru,en}` string arrays); a resolver produces plain-string `ConceptView`/`QuestionView` for the current language, consumed by components via hooks. Each task stays green: components move to resolver hooks as an identity pass first, then the schema flips to localized.

**Tech Stack:** React 18, TypeScript (strict), Zustand + persist, Tailwind, Vitest + Testing Library (jsdom), zod.

## Global Constraints

- Two languages only: `type Lang = 'ru' | 'en'`. No new runtime/dev dependencies (no i18next).
- Default language = `detectLang()` from `navigator.language` (`startsWith('ru')` → `'ru'`, else `'en'`; missing `navigator` → `'en'`). Persisted value overrides. Persist key is exactly `archmentor`.
- Language lives at `settings.lang`, changed only via existing `setSettings({ lang })` (no new store action) — mirrors how `settings.theme` works.
- Pattern/term names (`name`, `aka`), `Again/Hard/Good/Easy`, and enum values (`id`, `category`, `grade`, `type`) are language-neutral — never translated. `GRADE_LABEL` (Junior/Middle/Senior/Lead) is shared.
- Localized content fields: Concept `tagline/definition/problem/solution` (`Localized`), `pros/cons/tradeoffs/whenToUse/whenNotToUse` (`LocalizedList`), `codeExample.code` (`Localized`); Question `prompt/explanation` (`Localized`), `options` (`LocalizedList`), `code` (`Localized`). NOT localized: everything else. `options.ru` and `options.en` must have equal length.
- Phase A seeds `en` = `ru` (placeholder). Do NOT translate content prose in Phase A.
- After every task: `npx tsc --noEmit` clean and full suite (`npm test`) green.

---

### Task 1: Lang type, detection, and store setting

**Files:**
- Create: `src/i18n/lang.ts`
- Test: `src/i18n/lang.test.ts`
- Modify: `src/store/useStore.ts` (Settings interface line 9, initialData line 33)

**Interfaces:**
- Produces: `type Lang = 'ru' | 'en'`; `detectLang(): Lang`. Store `settings.lang: Lang`, default `detectLang()`.

- [ ] **Step 1: Write the failing test** — `src/i18n/lang.test.ts`

```ts
import { describe, it, expect, vi, afterEach } from 'vitest';
import { detectLang } from './lang';

afterEach(() => vi.unstubAllGlobals());

describe('detectLang', () => {
  it('returns ru for a Russian browser locale', () => {
    vi.stubGlobal('navigator', { language: 'ru-RU' });
    expect(detectLang()).toBe('ru');
  });
  it('returns en for a non-Russian locale', () => {
    vi.stubGlobal('navigator', { language: 'en-US' });
    expect(detectLang()).toBe('en');
  });
  it('returns en when navigator is unavailable', () => {
    vi.stubGlobal('navigator', undefined);
    expect(detectLang()).toBe('en');
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/i18n/lang.test.ts`
Expected: FAIL — cannot resolve `./lang`.

- [ ] **Step 3: Implement `src/i18n/lang.ts`**

```ts
export type Lang = 'ru' | 'en';

/** Pick the initial language from the browser; defaults to English. */
export function detectLang(): Lang {
  const nav = typeof navigator !== 'undefined' ? navigator.language : undefined;
  return nav && nav.toLowerCase().startsWith('ru') ? 'ru' : 'en';
}
```

- [ ] **Step 4: Add `lang` to the store**

In `src/store/useStore.ts`: add the import `import { detectLang, type Lang } from '@/i18n/lang';` (top, with other imports). Change the `Settings` interface (line 9) to include `lang`:

```ts
export interface Settings { theme: 'dark' | 'light'; lang: Lang; gradeFilter: Grade | 'all'; categoryFilter: Category | 'all'; }
```

Change `initialData()` settings (line 33) to seed `lang`:

```ts
  settings: { theme: 'dark', lang: detectLang(), gradeFilter: 'all', categoryFilter: 'all' },
```

- [ ] **Step 5: Add a store test for the default + persistence of lang** — append to `src/store/useStore.test.ts`

```ts
import { detectLang } from '@/i18n/lang';

describe('settings.lang', () => {
  it('defaults to the detected language and updates via setSettings', () => {
    expect(useStore.getState().settings.lang).toBe(detectLang());
    useStore.getState().setSettings({ lang: 'en' });
    expect(useStore.getState().settings.lang).toBe('en');
    useStore.getState().setSettings({ lang: 'ru' });
    expect(useStore.getState().settings.lang).toBe('ru');
  });
});
```

(If `useStore.test.ts` lacks the `describe/it/expect` or `useStore` imports, add them to match the file's existing header.)

- [ ] **Step 6: Verify**

Run: `npx vitest run src/i18n/lang.test.ts src/store/useStore.test.ts && npx tsc --noEmit`
Expected: PASS; tsc clean. (Note: `initialData()` also runs on `resetProgress`, so resetting progress re-detects language — consistent with theme, which also resets. Acceptable, out of scope to change.)

- [ ] **Step 7: Commit**

```bash
git add src/i18n/lang.ts src/i18n/lang.test.ts src/store/useStore.ts src/store/useStore.test.ts
git commit -m "feat: Lang type, browser detection, and store setting"
```

---

### Task 2: Message catalog and useT hook

**Files:**
- Create: `src/i18n/messages.ts`, `src/i18n/useT.ts`
- Test: `src/i18n/messages.test.ts`, `src/i18n/useT.test.tsx`

**Interfaces:**
- Produces: `messages: Record<Lang, Record<MessageKey, string>>`; `type MessageKey`; `translate(lang: Lang, key: MessageKey, vars?: Record<string, string | number>): string`; `useT(): (key: MessageKey, vars?: Record<string, string | number>) => string`.
- Consumes: `Lang` (Task 1), store `settings.lang`.

- [ ] **Step 1: Write the failing tests**

`src/i18n/messages.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { messages, translate } from './messages';

describe('messages catalog', () => {
  it('has identical key sets for ru and en', () => {
    const ru = Object.keys(messages.ru).sort();
    const en = Object.keys(messages.en).sort();
    expect(en).toEqual(ru);
  });
  it('has no empty strings', () => {
    for (const lang of ['ru', 'en'] as const)
      for (const [k, v] of Object.entries(messages[lang]))
        expect(v, `${lang}.${k}`).not.toBe('');
  });
});

describe('translate', () => {
  it('returns the string for the language', () => {
    expect(translate('en', 'nav.dashboard')).toBe('Dashboard');
    expect(translate('ru', 'nav.dashboard')).toBe('Дашборд');
  });
  it('interpolates {vars}', () => {
    expect(translate('en', 'common.counter', { index: 2, total: 5 })).toBe('2 / 5');
  });
});
```

`src/i18n/useT.test.tsx`:

```tsx
import { describe, it, expect, beforeEach } from 'vitest';
import { renderHook } from '@testing-library/react';
import { useT } from './useT';
import { useStore } from '@/store/useStore';

describe('useT', () => {
  beforeEach(() => useStore.getState().setSettings({ lang: 'ru' }));
  it('translates using the current store language', () => {
    const { result, rerender } = renderHook(() => useT());
    expect(result.current('nav.quiz')).toBe('Квиз');
    useStore.getState().setSettings({ lang: 'en' });
    rerender();
    expect(result.current('nav.quiz')).toBe('Quiz');
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run src/i18n/messages.test.ts src/i18n/useT.test.tsx`
Expected: FAIL — cannot resolve `./messages` / `./useT`.

- [ ] **Step 3: Implement `src/i18n/messages.ts`** (complete catalog — every UI string, both languages)

```ts
import type { Lang } from './lang';

const ru = {
  'nav.dashboard': 'Дашборд',
  'nav.learn': 'Учить',
  'nav.review': 'Повторение',
  'nav.quiz': 'Квиз',
  'nav.library': 'Библиотека',
  'nav.progress': 'Прогресс',
  'common.all': 'Все',
  'common.counter': '{index} / {total}',
  'common.skipToContent': 'К содержимому',
  'common.mainNav': 'Основная',
  'theme.toggle': 'Переключить тему',
  'lang.toggle': 'Сменить язык',
  'dashboard.title': 'Путь от Junior до Lead',
  'dashboard.streak': 'Серия дней',
  'dashboard.record': 'рекорд: {n}',
  'dashboard.dueToday': 'К повторению сегодня',
  'dashboard.startReview': 'Начать повторение →',
  'dashboard.testYourself': 'Проверить себя',
  'dashboard.quiz': 'Квиз',
  'dashboard.identifyPattern': 'Определи паттерн →',
  'dashboard.progressByGrade': 'Прогресс по грейдам',
  'dashboard.progressLabel': '{grade} — освоено {mastered}/{total}',
  'dashboard.continueLearning': 'Продолжить обучение →',
  'progress.title': 'Прогресс',
  'progress.quizAccuracy': 'Точность квизов',
  'progress.ofTotal': '{correct} из {total}',
  'progress.streak': 'Серия дней',
  'progress.conceptsMastered': 'Освоено концептов',
  'progress.outOf': 'из {total}',
  'progress.noAttempts': 'Пройдите квиз, чтобы увидеть точность и освоение.',
  'progress.masteryByGrade': 'Освоение по грейдам',
  'progress.masteryLabel': '{grade} — {mastered}/{total}',
  'progress.reset': 'Сбросить прогресс',
  'progress.resetConfirm': 'Сбросить весь прогресс?',
  'learn.title': 'Учить',
  'learn.emptyTitle': 'Нет карточек',
  'learn.emptyHint': 'Для выбранного фильтра карточек нет.',
  'learn.whenPrefix': 'Когда: {list}',
  'learn.next': 'Следующая →',
  'review.title': 'Повторение',
  'review.doneTitle': 'На сегодня всё повторено',
  'review.doneHint': 'Возвращайтесь завтра — карточки появятся по расписанию.',
  'review.learnNew': 'Учить новое',
  'review.remaining': 'осталось: {n}',
  'review.recallHint': 'Вспомните определение, затем переверните карточку и оцените себя.',
  'quiz.title': 'Квиз',
  'quiz.modeMix': 'Микс',
  'quiz.modeIdentify': 'Определи паттерн',
  'quiz.modeConcept': 'Теория',
  'quiz.modeTradeoff': 'Trade-offs',
  'quiz.emptyTitle': 'Нет вопросов',
  'quiz.emptyHint': 'Для выбранного режима вопросов пока нет.',
  'quiz.doneTitle': 'Готово!',
  'quiz.result': 'Результат: {correct} / {total}',
  'quiz.restart': 'Пройти заново',
  'quiz.explanation': 'Разбор',
  'quiz.next': 'Дальше →',
  'library.title': 'Библиотека',
  'library.searchPlaceholder': 'Поиск по названию…',
  'library.searchLabel': 'Поиск концептов',
  'library.emptyTitle': 'Ничего не найдено',
  'library.emptyHint': 'Попробуйте изменить запрос или фильтры.',
  'card.mastered': '✓ освоено',
  'concept.notFoundTitle': 'Концепт не найден',
  'concept.backToLibrary': '← В библиотеку',
  'concept.definition': 'Определение',
  'concept.problem': 'Проблема',
  'concept.solution': 'Решение',
  'concept.codeExample': 'Пример кода',
  'concept.pros': 'Плюсы',
  'concept.cons': 'Минусы',
  'concept.tradeoffs': 'Trade-offs',
  'concept.whenToUse': 'Когда применять',
  'concept.whenNotToUse': 'Когда не стоит',
  'concept.related': 'Похожие / путаемые',
  'flip.aria': 'Перевернуть карточку',
  'flip.definition': 'Определение',
  'flip.term': 'Термин',
  'flip.hint': 'Нажмите, чтобы увидеть ответ',
  'error.title': 'Что-то пошло не так',
} as const;

export type MessageKey = keyof typeof ru;

const en: Record<MessageKey, string> = {
  'nav.dashboard': 'Dashboard',
  'nav.learn': 'Learn',
  'nav.review': 'Review',
  'nav.quiz': 'Quiz',
  'nav.library': 'Library',
  'nav.progress': 'Progress',
  'common.all': 'All',
  'common.counter': '{index} / {total}',
  'common.skipToContent': 'Skip to content',
  'common.mainNav': 'Main',
  'theme.toggle': 'Toggle theme',
  'lang.toggle': 'Switch language',
  'dashboard.title': 'The path from Junior to Lead',
  'dashboard.streak': 'Day streak',
  'dashboard.record': 'record: {n}',
  'dashboard.dueToday': 'Due for review today',
  'dashboard.startReview': 'Start review →',
  'dashboard.testYourself': 'Test yourself',
  'dashboard.quiz': 'Quiz',
  'dashboard.identifyPattern': 'Identify the pattern →',
  'dashboard.progressByGrade': 'Progress by grade',
  'dashboard.progressLabel': '{grade} — mastered {mastered}/{total}',
  'dashboard.continueLearning': 'Continue learning →',
  'progress.title': 'Progress',
  'progress.quizAccuracy': 'Quiz accuracy',
  'progress.ofTotal': '{correct} of {total}',
  'progress.streak': 'Day streak',
  'progress.conceptsMastered': 'Concepts mastered',
  'progress.outOf': 'of {total}',
  'progress.noAttempts': 'Take a quiz to see accuracy and mastery.',
  'progress.masteryByGrade': 'Mastery by grade',
  'progress.masteryLabel': '{grade} — {mastered}/{total}',
  'progress.reset': 'Reset progress',
  'progress.resetConfirm': 'Reset all progress?',
  'learn.title': 'Learn',
  'learn.emptyTitle': 'No cards',
  'learn.emptyHint': 'No cards for the selected filter.',
  'learn.whenPrefix': 'When: {list}',
  'learn.next': 'Next →',
  'review.title': 'Review',
  'review.doneTitle': 'All caught up for today',
  'review.doneHint': 'Come back tomorrow — cards return on schedule.',
  'review.learnNew': 'Learn something new',
  'review.remaining': 'remaining: {n}',
  'review.recallHint': 'Recall the definition, then flip the card and rate yourself.',
  'quiz.title': 'Quiz',
  'quiz.modeMix': 'Mix',
  'quiz.modeIdentify': 'Identify the pattern',
  'quiz.modeConcept': 'Theory',
  'quiz.modeTradeoff': 'Trade-offs',
  'quiz.emptyTitle': 'No questions',
  'quiz.emptyHint': 'No questions for the selected mode yet.',
  'quiz.doneTitle': 'Done!',
  'quiz.result': 'Score: {correct} / {total}',
  'quiz.restart': 'Restart',
  'quiz.explanation': 'Explanation',
  'quiz.next': 'Next →',
  'library.title': 'Library',
  'library.searchPlaceholder': 'Search by name…',
  'library.searchLabel': 'Search concepts',
  'library.emptyTitle': 'Nothing found',
  'library.emptyHint': 'Try changing the query or filters.',
  'card.mastered': '✓ mastered',
  'concept.notFoundTitle': 'Concept not found',
  'concept.backToLibrary': '← Back to library',
  'concept.definition': 'Definition',
  'concept.problem': 'Problem',
  'concept.solution': 'Solution',
  'concept.codeExample': 'Code example',
  'concept.pros': 'Pros',
  'concept.cons': 'Cons',
  'concept.tradeoffs': 'Trade-offs',
  'concept.whenToUse': 'When to use',
  'concept.whenNotToUse': 'When not to use',
  'concept.related': 'Related / confusable',
  'flip.aria': 'Flip card',
  'flip.definition': 'Definition',
  'flip.term': 'Term',
  'flip.hint': 'Tap to reveal the answer',
  'error.title': 'Something went wrong',
};

export const messages: Record<Lang, Record<MessageKey, string>> = { ru, en };

export function translate(lang: Lang, key: MessageKey, vars?: Record<string, string | number>): string {
  let s: string = messages[lang][key];
  if (vars) for (const [k, v] of Object.entries(vars)) s = s.replace(`{${k}}`, String(v));
  return s;
}
```

- [ ] **Step 4: Implement `src/i18n/useT.ts`**

```ts
import { useStore } from '@/store/useStore';
import { translate, type MessageKey } from './messages';

export function useT() {
  const lang = useStore((s) => s.settings.lang);
  return (key: MessageKey, vars?: Record<string, string | number>) => translate(lang, key, vars);
}
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `npx vitest run src/i18n/messages.test.ts src/i18n/useT.test.tsx && npx tsc --noEmit`
Expected: PASS; tsc clean.

- [ ] **Step 6: Commit**

```bash
git add src/i18n/messages.ts src/i18n/useT.ts src/i18n/messages.test.ts src/i18n/useT.test.tsx
git commit -m "feat: i18n message catalog and useT hook"
```

---

### Task 3: LanguageToggle + localized CATEGORY_LABEL + Layout

**Files:**
- Create: `src/components/LanguageToggle.tsx`, `src/components/LanguageToggle.test.tsx`
- Modify: `src/lib/labels.ts` (CATEGORY_LABEL → localized), `src/app/Layout.tsx`

**Interfaces:**
- Consumes: `useT` (Task 2), store `settings.lang` + `setSettings`.
- Produces: `LanguageToggle` component; `CATEGORY_LABEL: Record<Lang, Record<Category, string>>`.

- [ ] **Step 1: Write the failing LanguageToggle test** — `src/components/LanguageToggle.test.tsx`

```tsx
import { describe, it, expect, beforeEach } from 'vitest';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { LanguageToggle } from './LanguageToggle';
import { useStore } from '@/store/useStore';

describe('LanguageToggle', () => {
  beforeEach(() => useStore.getState().setSettings({ lang: 'ru' }));
  it('shows the other language and switches on click', async () => {
    render(<LanguageToggle />);
    const btn = screen.getByRole('button', { name: /язык|language/i });
    expect(btn).toHaveTextContent('EN');
    await userEvent.click(btn);
    expect(useStore.getState().settings.lang).toBe('en');
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/components/LanguageToggle.test.tsx`
Expected: FAIL — cannot resolve `./LanguageToggle`.

- [ ] **Step 3: Implement `src/components/LanguageToggle.tsx`**

```tsx
import { useStore } from '@/store/useStore';
import { useT } from '@/i18n/useT';

export function LanguageToggle() {
  const lang = useStore((s) => s.settings.lang);
  const setSettings = useStore((s) => s.setSettings);
  const t = useT();
  const next = lang === 'ru' ? 'en' : 'ru';
  return (
    <button
      type="button"
      onClick={() => setSettings({ lang: next })}
      aria-label={t('lang.toggle')}
      className="rounded-lg px-2 py-1.5 text-sm font-medium text-muted hover:text-content focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-soft"
    >
      {next.toUpperCase()}
    </button>
  );
}
```

- [ ] **Step 4: Localize `CATEGORY_LABEL` in `src/lib/labels.ts`**

Replace the `CATEGORY_LABEL` export (keep `GRADE_ORDER` and `GRADE_LABEL` unchanged) with:

```ts
import type { Lang } from '@/i18n/lang';

export const CATEGORY_LABEL: Record<Lang, Record<Category, string>> = {
  ru: {
    solid: 'SOLID', creational: 'Порождающие', structural: 'Структурные',
    behavioral: 'Поведенческие', architecture: 'Архитектурные стили', tradeoff: 'Trade-offs',
  },
  en: {
    solid: 'SOLID', creational: 'Creational', structural: 'Structural',
    behavioral: 'Behavioral', architecture: 'Architecture styles', tradeoff: 'Trade-offs',
  },
};
```

Changing `CATEGORY_LABEL` from `Record<Category,string>` to `Record<Lang,Record<Category,string>>` type-breaks its four consumers, so fix them in the SAME task to keep tsc clean at task end (Step 4b).

- [ ] **Step 4b: Fix the four `CATEGORY_LABEL` consumers to index by lang**

In each, add `const lang = useStore((s) => s.settings.lang);` (import `useStore` if absent) and change `CATEGORY_LABEL[x]` → `CATEGORY_LABEL[lang][x]`:
- `src/features/library/ConceptCard.tsx:17` — `CATEGORY_LABEL[concept.category]` → `CATEGORY_LABEL[lang][concept.category]`.
- `src/features/library/ConceptPage.tsx:30` — `CATEGORY_LABEL[c.category]` → `CATEGORY_LABEL[lang][c.category]`.
- `src/features/learn/Learn.tsx:49` — `CATEGORY_LABEL[current.category]` → `CATEGORY_LABEL[lang][current.category]`.
- `src/features/library/Library.tsx:10-13` — the module-level `CATEGORY_OPTIONS` uses `CATEGORY_LABEL` before a lang exists; move it inside the component: delete the module const and, inside `Library()`, add `const lang = useStore((s) => s.settings.lang);` and `const CATEGORY_OPTIONS = [{ value: 'all' as const, label: 'Все' }, ...(Object.keys(CATEGORY_LABEL[lang]) as Category[]).map((c) => ({ value: c, label: CATEGORY_LABEL[lang][c] }))];`. (The `'Все'` literal and full option-typing are finalized in Task 4; this keeps tsc green now.)

These four files are rewritten again in Task 4 (UI-string translation); the minimal edit here just keeps the task green.

- [ ] **Step 5: Rewrite `src/app/Layout.tsx` to use `t()` and mount both toggles**

```tsx
import { useEffect } from 'react';
import { NavLink, Outlet } from 'react-router-dom';
import { useStore } from '@/store/useStore';
import { applyTheme } from './theme';
import { ThemeToggle } from '@/components/ThemeToggle';
import { LanguageToggle } from '@/components/LanguageToggle';
import { useT } from '@/i18n/useT';
import type { MessageKey } from '@/i18n/messages';

const NAV: { to: string; key: MessageKey; end?: boolean }[] = [
  { to: '/', key: 'nav.dashboard', end: true },
  { to: '/learn', key: 'nav.learn' },
  { to: '/review', key: 'nav.review' },
  { to: '/quiz', key: 'nav.quiz' },
  { to: '/library', key: 'nav.library' },
  { to: '/progress', key: 'nav.progress' },
];

export function Layout() {
  const theme = useStore((s) => s.settings.theme);
  const t = useT();
  useEffect(() => { applyTheme(theme); }, [theme]);

  return (
    <div className="min-h-screen flex flex-col">
      <a href="#main"
        className="sr-only focus:not-sr-only focus:absolute focus:left-4 focus:top-3 focus:z-50 focus:rounded-lg focus:bg-accent focus:px-3 focus:py-1.5 focus:text-white">
        {t('common.skipToContent')}
      </a>
      <header className="border-b border-surface-muted">
        <nav aria-label={t('common.mainNav')} className="max-w-5xl mx-auto flex items-center gap-1 px-4 py-3 overflow-x-auto">
          <span className="font-bold text-accent-soft mr-4">ArchMentor</span>
          {NAV.map((n) => (
            <NavLink key={n.to} to={n.to} end={n.end}
              className={({ isActive }) =>
                `px-3 py-1.5 rounded-lg text-sm whitespace-nowrap focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-soft ${isActive ? 'bg-surface-raised text-content' : 'text-muted hover:text-content'}`}>
              {t(n.key)}
            </NavLink>
          ))}
          <span className="ml-auto flex items-center gap-1"><LanguageToggle /><ThemeToggle /></span>
        </nav>
      </header>
      <main id="main" tabIndex={-1} className="flex-1 max-w-5xl w-full mx-auto px-4 py-8 focus:outline-none"><Outlet /></main>
    </div>
  );
}
```

- [ ] **Step 6: Verify**

Run: `npx vitest run src/components/LanguageToggle.test.tsx && npx tsc --noEmit`
Expected: LanguageToggle PASS; tsc clean (Task 3's Step 4b fixed the category consumers). Existing `App.test.tsx` may still pass if it matches nav by role/text — if it asserts the Russian nav literal and the default test lang is `en`, it will be updated in Task 6; note any failure and proceed (it is addressed there). To avoid a red suite between tasks, if `App.test.tsx` fails now, update its assertions to set `useStore.getState().setSettings({ lang: 'ru' })` before render.

- [ ] **Step 7: Commit**

```bash
git add src/components/LanguageToggle.tsx src/components/LanguageToggle.test.tsx src/lib/labels.ts src/app/Layout.tsx src/features/library/Library.tsx src/features/learn/Learn.tsx src/features/library/ConceptCard.tsx src/features/library/ConceptPage.tsx
git commit -m "feat: language toggle, localized category labels, translated layout"
```

---

### Task 4: Translate remaining UI strings in feature components

**Files (modify, replacing every Russian UI literal with `t(<key>)` — add `const t = useT();` and `import { useT } from '@/i18n/useT';` to each function component; `ErrorBoundary` uses `translate` since it is a class):**
- `src/features/dashboard/Dashboard.tsx`, `src/features/progress/Progress.tsx`, `src/features/learn/Learn.tsx`, `src/features/review/Review.tsx`, `src/features/quiz/Quiz.tsx`, `src/features/library/Library.tsx`, `src/features/library/ConceptCard.tsx`, `src/features/library/ConceptPage.tsx`, `src/components/FlipCard.tsx`, `src/app/ErrorBoundary.tsx`

**Interfaces:**
- Consumes: `useT` / `translate` (Task 2), `CATEGORY_LABEL[lang]` (Task 3). Content field reads (`concept.definition` etc.) are UNCHANGED here — still plain strings from the current schema. (Content moves to resolver hooks in Task 5.)

This task is mechanical: for each component, replace each Russian literal with `t('<key>')` (or `t('<key>', {vars})` for interpolated ones). The exact target text for every key is the catalog in Task 2 — that catalog is the source of truth; here map each literal to its key.

- [ ] **Step 1: Per-component key mapping** — apply exactly:

**Dashboard.tsx:** `Путь от Junior до Lead`→`t('dashboard.title')`; `Серия дней`→`t('dashboard.streak')`; `рекорд: {longest}`→`t('dashboard.record',{n:state.streak.longest})`; `К повторению сегодня`→`t('dashboard.dueToday')`; `Начать повторение →`→`t('dashboard.startReview')`; `Проверить себя`→`t('dashboard.testYourself')`; `Квиз`→`t('dashboard.quiz')`; `Определи паттерн →`→`t('dashboard.identifyPattern')`; `Прогресс по грейдам`→`t('dashboard.progressByGrade')`; ProgressBar label `${GRADE_LABEL[g]} — освоено ${p.mastered}/${p.total}`→`t('dashboard.progressLabel',{grade:GRADE_LABEL[g],mastered:p.mastered,total:p.total})`; `Продолжить обучение →`→`t('dashboard.continueLearning')`.

**Progress.tsx:** `Прогресс`→`t('progress.title')`; `Точность квизов`→`t('progress.quizAccuracy')`; `${correct} из ${total}`→`t('progress.ofTotal',{correct,total})`; `Серия дней`→`t('progress.streak')`; `Освоено концептов`→`t('progress.conceptsMastered')`; `из ${concepts.length}`→`t('progress.outOf',{total:concepts.length})`; `Пройдите квиз…`→`t('progress.noAttempts')`; `Освоение по грейдам`→`t('progress.masteryByGrade')`; ProgressBar label→`t('progress.masteryLabel',{grade:GRADE_LABEL[g],mastered:p.mastered,total:p.total})`; `Сбросить прогресс`→`t('progress.reset')`; `confirm('Сбросить весь прогресс?')`→`confirm(t('progress.resetConfirm'))`.

**Learn.tsx:** module-level `GRADE_OPTIONS` `label:'Все'`→ move options build inside component; all-option label `t('common.all')`; `Нет карточек`/hint→`t('learn.emptyTitle')`/`t('learn.emptyHint')`; `Учить`→`t('learn.title')`; counter `{index+1} / {deck.length}`→`t('common.counter',{index:index+1,total:deck.length})`; `Когда: {...}`→`t('learn.whenPrefix',{list:current.whenToUse.join('; ')})`; `Следующая →`→`t('learn.next')`.

**Review.tsx:** `Повторение` (both)→`t('review.title')`; `На сегодня всё повторено`→`t('review.doneTitle')`; done hint→`t('review.doneHint')`; cta label `Учить новое`→`t('review.learnNew')`; `осталось: {queue.length}`→`t('review.remaining',{n:queue.length})`; recall hint→`t('review.recallHint')`. (`Again/Hard/Good/Easy` stay literal.)

**Quiz.tsx:** `MODE_OPTIONS` labels → move inside component (or keep values, translate labels at render): `Микс`→`t('quiz.modeMix')`, `Определи паттерн`→`t('quiz.modeIdentify')`, `Теория`→`t('quiz.modeConcept')`, `Trade-offs`→`t('quiz.modeTradeoff')`; empty title/hint→`t('quiz.emptyTitle')`/`t('quiz.emptyHint')`; `Готово!`→`t('quiz.doneTitle')`; `Результат: ${correct} / ${total}`→`t('quiz.result',{correct,total})`; `Пройти заново`→`t('quiz.restart')`; `Квиз`→`t('quiz.title')`; counter→`t('common.counter',{index:i+1,total:deck.length})`; `Разбор`→`t('quiz.explanation')`; `Дальше →`→`t('quiz.next')`.

**Library.tsx:** `Библиотека`→`t('library.title')`; placeholder `Поиск по названию…`→`t('library.searchPlaceholder')`; aria-label `Поиск концептов`→`t('library.searchLabel')`; empty title/hint→`t('library.emptyTitle')`/`t('library.emptyHint')`; category options built in-component with `CATEGORY_LABEL[lang]` and all-label `t('common.all')`.

**ConceptCard.tsx:** `✓ освоено`→`t('card.mastered')`; `CATEGORY_LABEL[concept.category]`→`CATEGORY_LABEL[lang][concept.category]`.

**ConceptPage.tsx:** `Концепт не найден`→`t('concept.notFoundTitle')`; cta `← В библиотеку` and the top back-link→`t('concept.backToLibrary')`; section headings `Определение/Проблема/Решение/Пример кода`→`t('concept.definition'/'concept.problem'/'concept.solution'/'concept.codeExample')`; `List` titles `Плюсы/Минусы/Trade-offs/Когда применять/Когда не стоит`→`t('concept.pros'/'concept.cons'/'concept.tradeoffs'/'concept.whenToUse'/'concept.whenNotToUse')`; `Похожие / путаемые`→`t('concept.related')`; `CATEGORY_LABEL[c.category]`→`CATEGORY_LABEL[lang][c.category]`.

**FlipCard.tsx:** aria-label `Перевернуть карточку`→`t('flip.aria')`; `Определение`/`Термин`→`t('flip.definition')`/`t('flip.term')`; `Нажмите, чтобы увидеть ответ`→`t('flip.hint')`.

**ErrorBoundary.tsx (class — no hooks):** import `import { translate } from '@/i18n/messages'; import { useStore } from '@/store/useStore';` and render the heading as `{translate(useStore.getState().settings.lang, 'error.title')}`.

Representative before/after (pattern for all): Dashboard streak label —

```tsx
// before
<div className="text-sm text-muted">Серия дней</div>
// after
<div className="text-sm text-muted">{t('dashboard.streak')}</div>
```

Interpolated example — Progress accuracy detail —

```tsx
// before
<div className="text-xs text-muted mt-1">{correct} из {total}</div>
// after
<div className="text-xs text-muted mt-1">{t('progress.ofTotal', { correct, total })}</div>
```

- [ ] **Step 2: Verify no Russian UI literals remain in these components**

Run: `grep -rnE "[А-Яа-я]" src/features src/components/FlipCard.tsx src/app/ErrorBoundary.tsx --include="*.tsx" | grep -v "\.test\."`
Expected: no output (all UI Russian now flows through the catalog). Content field values are not literals here, so none appear.

- [ ] **Step 3: Verify tsc + suite**

Run: `npx tsc --noEmit && npm test 2>&1 | tail -5`
Expected: tsc clean. Some existing component tests that assert Russian text may fail because the default test language is `en`; those are updated in Task 6. If a test fails ONLY on a translated string, note it for Task 6 and continue; do not weaken the translation.

- [ ] **Step 4: Commit**

```bash
git add src/features src/components/FlipCard.tsx src/app/ErrorBoundary.tsx
git commit -m "feat: translate all feature-component UI strings"
```

---

### Task 5: Localize the content schema, migrate content, add the resolver

**Files:**
- Modify: `src/content/schema.ts`
- Create: `src/content/localize.ts`, `src/content/localize.test.ts`
- Modify (generated by migration script): `src/content/concepts/{solid,patterns,creational,structural,behavioral,architecture,tradeoffs}.ts`, `src/content/questions.ts`
- Modify: components' content reads → resolver hooks (see Step 6)

**Interfaces:**
- Produces: `type Localized = { ru: string; en: string }`; `type LocalizedList = { ru: string[]; en: string[] }`; localized `Concept`/`Question`; `type ConceptView`/`QuestionView` (plain strings); `localizeConcept(c, lang): ConceptView`; `localizeQuestion(q, lang): QuestionView`; hooks `useConcepts(): ConceptView[]`, `useConcept(id: string): ConceptView | undefined`, `useQuestions(): QuestionView[]`.
- Consumes: `Lang` (Task 1), store `settings.lang`.

- [ ] **Step 1: Change `src/content/schema.ts` to localized types**

Add near the top (after the enum schemas):

```ts
export const LocalizedSchema = z.object({ ru: z.string().min(1), en: z.string().min(1) });
export const LocalizedListSchema = z.object({ ru: z.array(z.string().min(1)).min(1), en: z.array(z.string().min(1)).min(1) });
export type Localized = z.infer<typeof LocalizedSchema>;
export type LocalizedList = z.infer<typeof LocalizedListSchema>;
```

Change `CodeSampleSchema.code` to `LocalizedSchema`; in `ConceptSchema` change `tagline/definition/problem/solution` to `LocalizedSchema` and `pros/cons/tradeoffs/whenToUse` to `LocalizedListSchema`, `whenNotToUse` to `LocalizedListSchema.optional()`; in `QuestionSchema` change `prompt/explanation` to `LocalizedSchema` and `options` to `LocalizedListSchema` (min length enforced by the array `.min(1)`; drop the old `.min(2)` — 2-option floor is preserved by content, and the localized options are validated for equal length below). Keep `name/aka/category/grade/related/tags/id/type/correctIndex/conceptId` as-is.

In `validateContent`, add after the correctIndex check (adapt it to `q.options.ru.length`):

```ts
  for (const q of questions) {
    if (q.options.ru.length !== q.options.en.length) {
      throw new Error(`question "${q.id}" options ru/en length mismatch`);
    }
    if (q.correctIndex >= q.options.ru.length) {
      throw new Error(`question "${q.id}" correctIndex ${q.correctIndex} out of range`);
    }
    if (q.conceptId && !ids.has(q.conceptId)) { /* unchanged */ }
  }
```

(Replace the existing correctIndex loop with this combined one.)

- [ ] **Step 2: Migrate all content via a one-shot vitest importer**

The content lives in TS modules (with `.join('\n')` code), so migrate by importing the runtime objects and re-emitting TS. Run this AFTER Step 1's schema change and BEFORE running `tsc` (between the two, content is plain at runtime but type-mismatched — vitest uses esbuild and ignores types, so the import still works). Create `src/content/__migrate.test.ts` with EXACTLY this content, run it once, then delete it.

```ts
import { it } from 'vitest';
import { writeFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { solid } from './concepts/solid';
import { patterns } from './concepts/patterns';
import { creational, creationalQuestions } from './concepts/creational';
import { structural, structuralQuestions } from './concepts/structural';
import { behavioral, behavioralQuestions } from './concepts/behavioral';
import { architecture, architectureQuestions } from './concepts/architecture';
import { tradeoffs, tradeoffsQuestions } from './concepts/tradeoffs';
import { questions as seedQuestions } from './questions';

/* eslint-disable @typescript-eslint/no-explicit-any */
const wrapS = (s: any) => ({ ru: s, en: s });
const wrapA = (a: any) => ({ ru: a, en: a });
const lc = (c: any) => ({
  ...c,
  tagline: wrapS(c.tagline), definition: wrapS(c.definition), problem: wrapS(c.problem), solution: wrapS(c.solution),
  codeExample: { ...c.codeExample, code: wrapS(c.codeExample.code) },
  pros: wrapA(c.pros), cons: wrapA(c.cons), tradeoffs: wrapA(c.tradeoffs), whenToUse: wrapA(c.whenToUse),
  ...(c.whenNotToUse ? { whenNotToUse: wrapA(c.whenNotToUse) } : {}),
});
const lq = (q: any) => ({
  ...q,
  prompt: wrapS(q.prompt), explanation: wrapS(q.explanation), options: wrapA(q.options),
  ...(q.code ? { code: { ...q.code, code: wrapS(q.code.code) } } : {}),
});

const q = (s: string) => JSON.stringify(s);
const emitStr = (v: string, ind: string) =>
  '[\n' + v.split('\n').map((l) => `${ind}  ${q(l)}`).join(',\n') + `,\n${ind}].join('\\n')`;
function emit(v: any, ind: string): string {
  if (typeof v === 'string') return v.includes('\n') ? emitStr(v, ind) : q(v);
  if (typeof v === 'number' || typeof v === 'boolean') return String(v);
  if (Array.isArray(v)) {
    if (v.every((x) => typeof x === 'number')) return '[' + v.join(', ') + ']';
    return '[\n' + v.map((x) => `${ind}  ${emit(x, ind + '  ')}`).join(',\n') + `,\n${ind}]`;
  }
  return '{\n' + Object.entries(v).map(([k, val]) => `${ind}  ${k}: ${emit(val, ind + '  ')}`).join(',\n') + `,\n${ind}}`;
}
const emitArray = (name: string, type: string, arr: any[]) =>
  `export const ${name}: ${type}[] = [\n${arr.map((o) => '  ' + emit(o, '  ')).join(',\n')},\n];\n`;

const ROOT = fileURLToPath(new URL('.', import.meta.url));
it('migrate content to localized shape (en=ru placeholder)', () => {
  const cImp = "import type { Concept } from '../schema';\n\n";
  const bImp = "import type { Concept, Question } from '../schema';\n\n";
  const w = (rel: string, body: string) => writeFileSync(ROOT + rel, body);
  w('concepts/solid.ts', cImp + emitArray('solid', 'Concept', solid.map(lc)));
  w('concepts/patterns.ts', cImp + emitArray('patterns', 'Concept', patterns.map(lc)));
  w('concepts/creational.ts', bImp + emitArray('creational', 'Concept', creational.map(lc)) + '\n' + emitArray('creationalQuestions', 'Question', creationalQuestions.map(lq)));
  w('concepts/structural.ts', bImp + emitArray('structural', 'Concept', structural.map(lc)) + '\n' + emitArray('structuralQuestions', 'Question', structuralQuestions.map(lq)));
  w('concepts/behavioral.ts', bImp + emitArray('behavioral', 'Concept', behavioral.map(lc)) + '\n' + emitArray('behavioralQuestions', 'Question', behavioralQuestions.map(lq)));
  w('concepts/architecture.ts', bImp + emitArray('architecture', 'Concept', architecture.map(lc)) + '\n' + emitArray('architectureQuestions', 'Question', architectureQuestions.map(lq)));
  w('concepts/tradeoffs.ts', bImp + emitArray('tradeoffs', 'Concept', tradeoffs.map(lc)) + '\n' + emitArray('tradeoffsQuestions', 'Question', tradeoffsQuestions.map(lq)));
  w('questions.ts', "import type { Question } from './schema';\n\n" + emitArray('questions', 'Question', seedQuestions.map(lq)));
});
```

Run: `npx vitest run src/content/__migrate.test.ts` → writes all 8 content files in localized form (`en` = copy of `ru`; prose emitted as quoted strings, multi-line code as `[...].join('\n')`). Then `rm src/content/__migrate.test.ts`.

Serialization correctness is guarded downstream: the placeholder invariant test (Step 3), `validateContent` (all fields non-empty, options lengths equal), and the existing content/component tests that assert specific Russian prose (e.g. `index.test.ts` distractor-name check, `ConceptPage` definition text) all fail if any value were corrupted.

- [ ] **Step 3: Round-trip verify the migration**

After running the migration and updating `schema.ts`, add to `src/content/index.test.ts` a check that every localized field's `ru` equals `en` (placeholder invariant) for the whole merged set, plus the existing `validateContent`:

```ts
it('all content is localized with en seeded from ru (phase A placeholder)', () => {
  for (const c of concepts) {
    expect(c.definition.ru).toBe(c.definition.en);
    expect(c.codeExample.code.ru).toBe(c.codeExample.code.en);
  }
  for (const q of questions) {
    expect(q.options.ru.length).toBe(q.options.en.length);
    expect(q.prompt.ru).toBe(q.prompt.en);
  }
});
```

Run: `npx vitest run src/content/index.test.ts`
Expected: PASS (schema parses the localized content; placeholder invariant holds).

- [ ] **Step 4: Write the resolver test** — `src/content/localize.test.ts`

```ts
import { describe, it, expect } from 'vitest';
import { concepts, questions } from './index';
import { localizeConcept, localizeQuestion } from './localize';

describe('resolver', () => {
  it('localizeConcept returns plain strings for the language', () => {
    const c = concepts[0];
    const v = localizeConcept(c, 'ru');
    expect(v.definition).toBe(c.definition.ru);
    expect(typeof v.definition).toBe('string');
    expect(v.pros).toEqual(c.pros.ru);
    expect(v.codeExample.code).toBe(c.codeExample.code.ru);
    expect(v.name).toBe(c.name); // shared field passes through
  });
  it('localizeQuestion keeps options length and correctIndex valid', () => {
    const q = questions[0];
    const v = localizeQuestion(q, 'en');
    expect(v.options).toEqual(q.options.en);
    expect(v.correctIndex).toBeLessThan(v.options.length);
  });
});
```

- [ ] **Step 5: Implement `src/content/localize.ts`**

```ts
import type { Concept, Question, Localized, LocalizedList } from './schema';
import type { Lang } from '@/i18n/lang';
import { useStore } from '@/store/useStore';
import { concepts, questions, getConcept } from './index';

const s = (l: Localized, lang: Lang) => l[lang];
const a = (l: LocalizedList, lang: Lang) => l[lang];

export interface ConceptView {
  id: string; name: string; aka?: string[]; category: Concept['category']; grade: Concept['grade'];
  tagline: string; definition: string; problem: string; solution: string;
  codeExample: { lang: 'typescript'; code: string; highlightLines?: number[] };
  pros: string[]; cons: string[]; tradeoffs: string[]; whenToUse: string[]; whenNotToUse?: string[];
  related: string[]; tags?: string[];
}

export interface QuestionView {
  id: string; type: Question['type']; category: Question['category']; grade: Question['grade'];
  prompt: string; code?: { lang: 'typescript'; code: string; highlightLines?: number[] };
  options: string[]; correctIndex: number; explanation: string; conceptId?: string;
}

export function localizeConcept(c: Concept, lang: Lang): ConceptView {
  return {
    id: c.id, name: c.name, aka: c.aka, category: c.category, grade: c.grade,
    tagline: s(c.tagline, lang), definition: s(c.definition, lang),
    problem: s(c.problem, lang), solution: s(c.solution, lang),
    codeExample: { lang: c.codeExample.lang, code: s(c.codeExample.code, lang), highlightLines: c.codeExample.highlightLines },
    pros: a(c.pros, lang), cons: a(c.cons, lang), tradeoffs: a(c.tradeoffs, lang),
    whenToUse: a(c.whenToUse, lang), whenNotToUse: c.whenNotToUse ? a(c.whenNotToUse, lang) : undefined,
    related: c.related, tags: c.tags,
  };
}

export function localizeQuestion(q: Question, lang: Lang): QuestionView {
  return {
    id: q.id, type: q.type, category: q.category, grade: q.grade,
    prompt: s(q.prompt, lang),
    code: q.code ? { lang: q.code.lang, code: s(q.code.code, lang), highlightLines: q.code.highlightLines } : undefined,
    options: a(q.options, lang), correctIndex: q.correctIndex, explanation: s(q.explanation, lang), conceptId: q.conceptId,
  };
}

export function useConcepts(): ConceptView[] {
  const lang = useStore((st) => st.settings.lang);
  return concepts.map((c) => localizeConcept(c, lang));
}
export function useConcept(id: string): ConceptView | undefined {
  const lang = useStore((st) => st.settings.lang);
  const c = getConcept(id);
  return c ? localizeConcept(c, lang) : undefined;
}
export function useQuestions(): QuestionView[] {
  const lang = useStore((st) => st.settings.lang);
  return questions.map((q) => localizeQuestion(q, lang));
}
```

- [ ] **Step 6: Wire component content reads to the resolver** (UI strings are already `t()` from Task 4; now switch content sources):
  - **Learn.tsx:** replace `import { concepts, getConcept } from '@/content/index'` with `import { useConcepts, useConcept } from '@/content/localize'`. Replace `deck` computation: use `useConcepts()` for the filtered list and `useConcept(conceptId)` for the single-concept path. `current` is a `ConceptView` — field reads unchanged.
  - **Review.tsx:** keep `concepts`/`getConcept` for the store selector (`selectReviewQueue` needs raw `Concept[]` of shared fields — it only reads `id`), but resolve the DISPLAYED concept: `const concept = currentId ? useConcept(currentId) : undefined`. (`useConcept` is a hook — call unconditionally: `const view = useConcept(currentId ?? '')` then `const concept = currentId ? view : undefined`.)
  - **Library.tsx:** `const list = useConcepts()`; filter over `list` (fields `name/aka/tagline` are now plain strings on the view). Pass `ConceptView` to `ConceptCard`.
  - **ConceptCard.tsx:** change prop type from `Concept` to `ConceptView` (`import type { ConceptView } from '@/content/localize'`).
  - **ConceptPage.tsx:** `const c = useConcept(conceptId ?? '')` (call hook unconditionally); related names via `useConcepts()` lookup or keep a raw `getConcept(r)?.name` (name is shared — `getConcept` still fine for the name). Use `useConcept` for the main concept.
  - **Quiz.tsx:** replace `import { questions as allQuestions } from '@/content/index'` with `const allQuestions = useQuestions()` inside the component; `selectQuestions`/`isCorrect`/`scoreSession` operate on `QuestionView` (same shape fields they read: `type`, `options`, `correctIndex`, `id`). Confirm `src/domain/quiz/selection.ts` typing accepts `QuestionView` — if it imports `Question`, change its parameter types to a structural subset or `QuestionView`; simplest: change `selection.ts` to accept the view shape (it only reads `type`, `id`, `options`, `correctIndex`).
  - **Dashboard.tsx / Progress.tsx:** unchanged content-wise — they pass raw `concepts` to store selectors (`selectGradeProgress`, `isMastered`) which read only shared fields. Leave `import { concepts } from '@/content/index'` for those selectors.

- [ ] **Step 7: Verify tsc + suite**

Run: `npx tsc --noEmit && npm test 2>&1 | tail -5`
Expected: tsc clean. Content-consuming components compile against `ConceptView`/`QuestionView`. Some component tests still assert Russian and may fail (fixed in Task 6).

- [ ] **Step 8: Commit**

```bash
git add src/content/schema.ts src/content/localize.ts src/content/localize.test.ts src/content/index.test.ts src/content/concepts src/content/questions.ts src/features src/domain/quiz/selection.ts
git commit -m "feat: localize content schema, migrate content (en=ru placeholder), add resolver"
```

---

### Task 6: Update tests for language + EN smoke tests + final verification

**Files:**
- Modify: existing component/app tests that assert Russian strings (`src/app/App.test.tsx`, `src/features/learn/Learn.test.tsx`, `src/features/quiz/Quiz.test.tsx`, `src/features/library/Library.test.tsx`, `src/components/components.test.tsx` if it asserts RU).
- Create: `src/i18n/i18n.smoke.test.tsx` (EN-mode render of key screens).

**Interfaces:** Consumes everything above.

- [ ] **Step 1: Make existing tests language-deterministic**

In each failing test file, set the language before rendering. Add at the top of the relevant `describe`/`beforeEach`:

```tsx
import { useStore } from '@/store/useStore';
beforeEach(() => useStore.getState().setSettings({ lang: 'ru' }));
```

Then keep the existing Russian assertions (now they hold because lang is forced to `ru`). For assertions on content prose (e.g. Library `getByText('Strategy')` — `name` is shared, unaffected; ConceptPage `/семей额алгоритмов/` — now resolved RU, still holds under `lang:'ru'`).

- [ ] **Step 2: Add EN smoke test** — `src/i18n/i18n.smoke.test.tsx`

```tsx
import { describe, it, expect, beforeEach } from 'vitest';
import { render, screen } from '@testing-library/react';
import { RouterProvider, createMemoryRouter } from 'react-router-dom';
import { Layout } from '@/app/Layout';
import { Dashboard } from '@/features/dashboard/Dashboard';
import { Library } from '@/features/library/Library';
import { useStore } from '@/store/useStore';

function renderAt(path: string) {
  const router = createMemoryRouter(
    [{ path: '/', element: <Layout />, children: [
      { index: true, element: <Dashboard /> },
      { path: 'library', element: <Library /> },
    ] }], { initialEntries: [path] });
  return render(<RouterProvider router={router} />);
}

describe('English UI', () => {
  beforeEach(() => useStore.getState().setSettings({ lang: 'en' }));
  it('renders nav and dashboard in English', () => {
    renderAt('/');
    expect(screen.getByRole('link', { name: 'Library' })).toBeInTheDocument();
    expect(screen.getByText('The path from Junior to Lead')).toBeInTheDocument();
  });
  it('renders the library heading in English', () => {
    renderAt('/library');
    expect(screen.getByRole('heading', { name: 'Library' })).toBeInTheDocument();
  });
});
```

- [ ] **Step 3: Full verification**

Run: `npx tsc --noEmit && npm test 2>&1 | tail -5 && npm run build 2>&1 | tail -6`
Expected: tsc clean; all tests pass; build succeeds with no chunk-size warning.

- [ ] **Step 4: Manual browser check (coordinator)** — drive the app, toggle RU↔EN, confirm the entire UI switches and content shows Russian in both (EN placeholder) with no missing strings.

- [ ] **Step 5: Commit**

```bash
git add src/app/App.test.tsx src/features src/components src/i18n/i18n.smoke.test.tsx
git commit -m "test: language-deterministic tests and English UI smoke tests"
```

---

## Self-Review Notes

- **Spec coverage:** §2 lang/detect/store → Task 1; §3 catalog/useT/toggle/CATEGORY_LABEL/UI translation → Tasks 2-4; §4 localized schema + resolver + hooks → Task 5; §5 mechanical migration (en=ru) → Task 5 Step 2-3; §6 Phase A boundary (no content translation) → enforced by Global Constraints + placeholder invariant test; §7 tests → Tasks 1,2,5,6. Phase B (EN generation) intentionally excluded.
- **Green-between-tasks:** Task 3 folds the four `CATEGORY_LABEL` consumers (Step 4b) so tsc is clean at its end; component tests asserting Russian are made language-deterministic in Task 6 — the plan flags that they may be red between Tasks 3-5 and fixes them in Task 6 (acceptable within an SDD run that reviews at task end, but if strict per-task green is required, apply the Task-6 Step-1 `beforeEach(lang:'ru')` edits to a test as soon as its component is translated).
- **Type consistency:** `Localized`/`LocalizedList`, `ConceptView`/`QuestionView`, `localizeConcept`/`localizeQuestion`, `useConcepts`/`useConcept`/`useQuestions`, `detectLang`, `translate`/`useT`, `MessageKey` — names identical across tasks. `options` becomes `LocalizedList`; `correctIndex` indexes `options.ru`/`options.en` (equal length enforced).
- **selection.ts:** Task 5 Step 6 notes its param types may need widening to the view shape (reads only `type`, `id`, `options`, `correctIndex`).
- **Known behavior:** `resetProgress` re-detects language (same as it re-defaults theme) — out of scope to change.
